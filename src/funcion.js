
/**
 *  Esta funcion registra un usuario
 * @param {String} nombre El nombre del usuario
 * @param {Number|String} edad La edad del usuario
 * @param {Boolean} [estudiante] Si el usuario es estudiante. los [] indican valor opcional
 * @param {Array} datos Datos adisionales del usuario 
 * @returns {Boolean} Retorn true si es verdad
 */

const regiterUser = (nombre, edad, estudiante, datos) => {
  console.log('Se registro BD falsa')
  return true
}
export { regiterUser }
